# AutoCAD MCP server dependencies.
#   pip install -r requirements.txt
#
# Both of these were already present on this machine; the pins record what the
# server was verified against on 2026-07-30.
mcp==1.27.1
pywin32==311
